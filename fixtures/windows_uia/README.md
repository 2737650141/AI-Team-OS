# Windows UI Automation Test Fixture

This fixture is intentionally excluded from normal CI. Run it manually with the project Python
environment during M5-A desktop integration acceptance. It exposes a button, textbox, password
field, checkbox, list and simulated elevation status without touching a user's production apps.
