# Visual Desktop Test App

Local, deterministic Win32 fixture for M5-B. It contains native UIA controls plus parent-drawn
canvas controls that intentionally have no child accessibility nodes. The coloured canvas regions
exercise local CV, spatial grounding, ambiguity, target movement, modal changes, prompt-injection
text, icon grounding, dangerous controls, and password redaction. It uses no network and contains
no user data.
